# Politique de régulation asymétrique :
#   OFF rapide (protection sur-chloration) : on_value_range above setpoint
#     → l'électrolyseur se coupe dès le franchissement supérieur (réaction en secondes).
#   ON lent (filtre bruit capteur) : interval 1min + g_redox_stable_minutes >= 30
#     → l'électrolyseur ne démarre qu'après 30 min de tendance Redox non-montante
#       (g_redox_trend_state <= 0) avec ORP < setpoint - 30 mV. Les dips transitoires
#       (bruit, agitation) sont ignorés.
# Le bloc OFF de l'interval sert de filet pour les cas sans franchissement
# (ex. passage Off → Auto alors qu'ORP est déjà au-dessus du setpoint).

globals:
  - id: g_redox_stable_minutes
    type: int
    restore_value: true
    initial_value: '0'

select:
  - platform: template
    name: Mode Electrolyseur
    id: electrolyser_mode
    optimistic: True
    options:
      - "Off"
      - Auto
      - Forcé
    restore_value: True
    initial_option: "Off"
    entity_category: config
    icon: mdi:chemical-weapon
    on_value:
      - lambda: |-
          if (id(electrolyser_mode).state == "Off") {
            id(electrolyser).turn_off();
          } else if (id(electrolyser_mode).state == "Forcé") {
            id(electrolyser).turn_on();
          } else {
            if (id(pool_redox).state > id(redox_setpoint).state) {
              id(electrolyser).turn_off();
            } else {
              id(electrolyser).turn_on();
            }
          }

number:
  - platform: template
    name: Consigne Redox
    id: redox_setpoint
    optimistic: True
    unit_of_measurement: mV
    device_class: voltage
    step: 10
    min_value: 680
    max_value: 760
    restore_value: True
    initial_value: 730
    entity_category: config

sensor:
  - id: !extend pool_redox
    on_value_range:
      - above: !lambda return id(redox_setpoint).state;
        then:
          - lambda: |-
              if (id(electrolyser_mode).state == "Auto") {
                id(electrolyser).turn_off();
              }

interval:
  - interval: 1min
    then:
      - if:
          condition:
            lambda: 'return (int)id(g_redox_trend_state) <= 0;'
          then:
            - lambda: 'id(g_redox_stable_minutes) += 1;'
          else:
            - lambda: 'id(g_redox_stable_minutes) = 0;'
      - delay: 10s
      - lambda: |-
          if (id(electrolyser_mode).state != "Auto") return;

          if (!id(pump).state || (id(pump_uptime).state < id(pump_uptime_delay).state)) {
            return;
          }

          float current = id(pool_redox).state;
          float minr = id(redox_setpoint).state - 30;
          float maxr = id(redox_setpoint).state;
          int stable_minutes = id(g_redox_stable_minutes);

          if ((current > maxr) && (id(electrolyser).state)) {
            id(electrolyser).turn_off();
            return;
          }

          if ((current < minr) && (!id(electrolyser).state)) {
            if (stable_minutes >= 30) {
              id(electrolyser).turn_on();
            }
            return;
          }
